apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicio-notificaciones
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicio-notificaciones
  template:
    metadata:
      labels:
        app: servicio-notificaciones
    spec:
      containers:
        - name: servicio-notificaciones
          image: arka-lite-servicio-notificaciones:latest
          ports:
            - containerPort: 8080
  #          readinessProbe:
  #            httpGet: {path: /, port:80}
  #            initialDelaySeconds: 5
  #          livenessProbe:
  #            httpGet: {path: /, port:80}
  #            periodSeconds: 10

